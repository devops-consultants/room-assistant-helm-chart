apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "room-assistant.fullname" . }}-test-connection"
  labels:
    {{- include "room-assistant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "room-assistant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
